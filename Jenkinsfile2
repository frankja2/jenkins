pipeline {
    agent none
    stages {
        stage('Run in Docker') {
            agent {
                docker {
                    image 'registry-gitlab.goodylabs.com/krka/singlepill-api:v2'
                }
            }
            steps {
                sh 'ssh'
            }
        }
    }
}

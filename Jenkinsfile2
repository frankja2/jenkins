pipeline {
    agent none
    stages {
        stage('Run in Docker') {
            agent {
                label 'linux' // Use Linux nodes with Docker installed
                docker {
                    image 'node:20.17.0-alpine3.20'
                }
            }
            steps {
                sh 'node --version'
            }
        }
    }
}

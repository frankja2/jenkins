pipeline {
    agent none
    stages {
        stage('Run in Docker') {
            agent {
                docker {
                    image 'ubuntu:24.04'
                }
            }
            steps {
                sh 'ssh'
            }
        }
    }
}

pipeline {
    agent none
    stages {
        stage('Run in Docker') {
            agent {
                label 'linux' // Use Linux nodes with Docker installed
                docker {
                    image 'ubuntu:24.04'
                }
            }
            steps {
                sh 'ssh'
            }
        }
    }
}
